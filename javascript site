function getPrice() {
  var instrument = document.getElementById("instrument").value;

  // Effectuez ici les appels AJAX ou les requêtes nécessaires pour obtenir le prix du instrument sélectionné

  // Supposons que le prix est récupéré dans une variable nommée "price"
  var price = 100;

  document.getElementById("price").innerText = "Le prix actuel est : " + price + " $";
}

// Utilisez ici une bibliothèque de graphiques telle que Chart.js pour créer le graphique historique

// Ajoutez ici les fonctions nécessaires pour effectuer les simulations de rendements
